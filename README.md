# Assignment-tracker-API
An api designed to track student's assignments
Endpoints list — POST /auth/register, POST /auth/login, GET /assignments, GET /assignments/id, PATCH assignments/id, DELETE /assignments/id, POST /assignments/id
Tech stack — Node, Express, MongoDB, JWT, bcryptJS
Setup/env vars needed
